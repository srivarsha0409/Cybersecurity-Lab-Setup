Symmetric Encryption: The same key is used fro encryption and decryption. Example:AES,

Asymmetric Encryption: Uses a public key for encryption and private key for decryption Example:RSA

MD5: MD5 is a hashing algorithm that generates a 128-bit hash value.

SHA256: SHA256 is a secure hashing algorithm that generates a 256-bit hash value.

SSL/TLS: SSl/TLS protocols provide secure communication over networks and are commonly used in HTTPS websites
